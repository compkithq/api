type Competition {
  id: ID!
  name: String
  venue: Venue
}

type Venue {
  id: ID!
  competitions: [Competition]
  description: String
  name: String
}

type Query {
  competitions(limit: Int, offset: Int): [Competition]
  competition(id: ID!): Competition
}
